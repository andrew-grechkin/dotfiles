# vim: filetype=sh foldmethod=marker
# shellcheck shell=bash

# when lightdm runs this script the ssh agent is not yet started, but I expect it will be started later and create the socket

# => gcr-ssh-agent ------------------------------------------------------------------------------------------------ {{{1

# export SSH_AUTH_SOCK="${XDG_RUNTIME_DIR}/gcr/ssh"

# => keyring-ssh-agent -------------------------------------------------------------------------------------------- {{{1

# this doesn't exist any more since gnome-keyring 1:46.0-1
# export SSH_AUTH_SOCK="${XDG_RUNTIME_DIR}/keyring/ssh"

# => gpg-agent-ssh ------------------------------------------------------------------------------------------------ {{{1

#export SSH_AGENT_PID=
export SSH_AUTH_SOCK="${XDG_RUNTIME_DIR}/gnupg/S.gpg-agent.ssh"

# => openssh-agent ------------------------------------------------------------------------------------------------ {{{1

# export SSH_AUTH_SOCK="${XDG_RUNTIME_DIR}/S.ssh-agent"

# => activate mise shims globally --------------------------------------------------------------------------------- {{{1

[[ -x "$(command -v mise)" ]] && eval "$(mise activate --shims)"

# => use default Perl + modules globally -------------------------------------------------------------------------- {{{1

[[ -r "$HOME/.local/lib/mise-perl-global.rc" ]] && source "$HOME/.local/lib/mise-perl-global.rc"

# => use default env ---------------------------------------------------------------------------------------------- {{{1

[[ -r "$HOME/.config/shell/env" ]] && source "$HOME/.config/shell/env"
