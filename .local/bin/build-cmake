#!/usr/bin/env bash

set -Eeuo pipefail

CWD="${1:-$(pwd)}"

BUILD_TYPE=${BUILD_TYPE:-Debug}
DIR=".build-$BUILD_TYPE"

mkdir -p "$CWD/$DIR" && pushd "$CWD/$DIR"
cmake -DCMAKE_BUILD_TYPE="$BUILD_TYPE" "$CWD"
popd

make -j -C "$CWD/$DIR"
