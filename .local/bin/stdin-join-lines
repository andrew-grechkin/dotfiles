#!/usr/bin/env perl

use v5.16;
use strict;
use warnings;

print join (' ', map {chomp; length $_ ? $_ : +()} <>); ## no critic [InputOutput::ProhibitJoinedReadline]

__END__

# perl is much more efficient here
##!/bin/bash
#
#set -Eeuo pipefail
#
## while read LINE; do
## 	echo -n " ${(q)LINE}"
## done
#
#mapfile -t DATA
#
## do not quote DATA to force bash eliminate empty lines
## shellcheck disable=SC2086
#echo ${DATA[*]}
