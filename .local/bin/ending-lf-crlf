#!/usr/bin/env perl
# man: PerlIO

use v5.28;
use autodie;
use open ':std', 'IO' => ':locale';
use strict;
use utf8;
use warnings;

binmode (STDOUT, ':crlf');

while (defined (my $line = <>)) {
    chomp ($line);
} continue {
    say $line;
}

__END__
