#!/usr/bin/env bash

set -Eeuo pipefail

chosen="$(perl "${HOME}/.local/bin/grep-emoji" | LC_ALL=en_US.utf8 fzf)"

[[ "$chosen" != "" ]] || exit

# shellcheck disable=2001
echo "$chosen" | sed 's/ > .*//'
