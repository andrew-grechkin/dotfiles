#!/bin/bash

set -Eeuo pipefail

DIR="$(dirname "$0")"
PATH="$DIR:$PATH"

chosen="$(perl "${DIR}/grep-emoji" | LC_ALL="en_US.UTF-8" fzf)"

[[ "$chosen" != "" ]] || exit

# shellcheck disable=2001
#echo "$chosen" | sed 's/ > .*//'
echo -n "$chosen" | sed 's/ > .*//' | clipcopy
