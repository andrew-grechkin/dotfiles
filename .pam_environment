# vim: filetype=conf foldmethod=marker

# => defaults ---------------------------------------------------------------------------------------------------- {{{1

EDITOR              DEFAULT=vim
PAGER               DEFAULT=less
VISUAL              DEFAULT=vim

# => xdg --------------------------------------------------------------------------------------------------------- {{{1

XDG_CACHE_HOME      DEFAULT="@{HOME}/.cache"
XDG_CONFIG_HOME     DEFAULT="@{HOME}/.config"
XDG_DATA_HOME       DEFAULT="@{HOME}/.local/share"
XDG_STATE_HOME      DEFAULT="@{HOME}/.local/state"

# => shell ------------------------------------------------------------------------------------------------------- {{{1

ZDOTDIR             DEFAULT="${XDG_CONFIG_HOME}/zsh"
FUSE3_P7ZIP_FORMATS DEFAULT="Iso:Udf:*:APM:GPT"

# => wine -------------------------------------------------------------------------------------------------------- {{{1

WINEPREFIX          DEFAULT="${XDG_CACHE_HOME}/wine/default-x32"
WINEARCH            DEFAULT=win32

# => X11 settings ------------------------------------------------------------------------------------------------ {{{1

#XINITRC             DEFAULT="${XDG_CONFIG_HOME}/.config/x11/xinitrc"
#XSERVERRC           DEFAULT="${XDG_CONFIG_HOME}/x11/xserverrc"

### this might break lightdm (and xrdp)
#XAUTHORITY          DEFAULT="${XDG_RUNTIME_DIR}/Xauthority"
#ICEAUTHORITY        DEFAULT="${XDG_RUNTIME_DIR}/ICEauthority"

# => does not work ----------------------------------------------------------------------------------------------- {{{1

# GTK_RC_FILES        DEFAULT="${XDG_CONFIG_HOME}/x11/gtk1rc"
# GTK2_RC_FILES       DEFAULT="${XDG_CONFIG_HOME}/x11/gtk2rc"
# ERRFILE             DEFAULT="${XDG_CACHE_HOME}/x11/xsession-errors"

# => kde --------------------------------------------------------------------------------------------------------- {{{1

KDEHOME             DEFAULT="${XDG_CONFIG_HOME}/kde"

# => misc -------------------------------------------------------------------------------------------------------- {{{1

ANSIBLE_HOME           DEFAULT="${XDG_CONFIG_HOME}/ansible"
GROFF_TMPDIR        DEFAULT="/tmp"
PYTHON_KEYRING_BACKEND DEFAULT="keyring.backends.null.Keyring"
RIPGREP_CONFIG_PATH DEFAULT="${XDG_CONFIG_HOME}/ripgreprc"
USRTMP              DEFAULT="@{HOME}/.local/no-cow/tmp"
VAGRANT_DOTFILE_PATH   DEFAULT="${XDG_DATA_HOME}/vagrant"
