extends bash

snippet #!
	#!/usr/bin/env zsh

	set -Eeuo pipefail

snippet case
	case ${1:word} in
		${2:pattern})
			${0};;
	esac

snippet select
	select ${1:answer} in ${2:choices}; do
		${0:${VISUAL}}
	done

snippet always
	{ ${1:try} } always { ${0:always} }
